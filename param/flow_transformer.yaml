# Settings for the flow transformer	
flow_transformer:

	#fov of the camera (in radians)
	fov: 0.733

	# Minimum altitude to run the optical flow sensor
	min_estimation_altitude: 0.08

  # Timeout for updates from tf
  tf_timeout: 1.0

  # Width/height of pixels 
  